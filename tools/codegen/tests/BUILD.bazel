load("//tools/codegen:codegen.bzl", "codegen_test")

codegen_test(
    name = "gen-test",
    expected = "gen-test.expected",
    data = ["data.yaml"],
    srcs = ["test.template"],
    overrides = ["over=rides", "zoo=zebra"],
)
