load("//tools/codegen:codegen.bzl", "codegen_test")

codegen_test(
    name = "gen-test",
    srcs = ["test.template"],
    data = ["data.yaml"],
    expected = "gen-test.expected",
    overrides = [
        "over=rides",
        "zoo=zebra",
    ],
)
