load("//bazel/appengine:defs.bzl", "go_appengine_deploy")

go_appengine_deploy(
    name = "deploy",
    config = "deploy/app.yaml",
    entry = "github.com/enfabrica/enkit/astore/server/cmd/astore_auth",
    gomod = "//:go.mod",
    gosum = "//:go.sum",
    project = "astore-284118",
    deps = [
        "//astore/server/cmd/astore_auth",
    ],
)
